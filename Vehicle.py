class Vehicle:

    def __init__(self, latvelocity, linvelocity, radius, startpostions):

        self.latvelocity = latvelocity
        self.linvelocity = linvelocity

        self.radius = radius
        self.startpostions = startpostions

    def trajectory(self):
        """
        Determine the trajectory of the vehicle. with starting position and velocity
        """
        return

    def endurance(self):
        """
        Determine the time that the HAPS can be operated
        """
        return